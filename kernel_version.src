// kernel_version
// This script retrieves the kernel_router.so version of a router using its IP address.


import_code("/Tools/src/srm_lib/params.src")

// Check the params, send help command
add_param("ip_address", true, false, "The IP address of the router")
handle_help()

router = get_router(params_map.ip_address)
print(router.kernel_version)